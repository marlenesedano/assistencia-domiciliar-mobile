import { View } from "react-native";

export function Line() {
  return (
    <View
      style={{
        borderBottomColor: "#BDBDBD",
        borderBottomWidth: 1,
        marginTop: 12,
        marginBottom: 12,
      }}
    />
  );
}
